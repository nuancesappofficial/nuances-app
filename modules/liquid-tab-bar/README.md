# liquid-tab-bar (local Expo Module)

This module exposes an iOS-only SwiftUI liquid glass tab bar to React Native.

## Files

- `expo-module.config.json`: Expo module declaration for autolinking.
- `ios/`: Native iOS implementation (`LiquidTabBarModule`, `LiquidTabBarView`, podspec).
- `src/`: React Native wrapper component.
